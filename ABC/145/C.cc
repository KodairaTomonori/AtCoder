
#include <bits/stdc++.h>
#define rep(i, n) for (int i=0; i < n; i ++)
#define rep1(i, n) for (int i=1; i <= n; i ++)
#define _min(x, y) x = min(x, y)
#define _max(x, y) x = max(x, y)
using namespace std;

pair<int, int> P;

int main() {
  int N; cin >> N;
  vector<pair> towns;
  rep(i, N) {
    int x, y; cin >> x >> y;
    towns.push_back(P(x, y));
  }
  rep(i, N) {
    rep(j, N) {
      sqrt(towns[

    }

  }

  return 0;
}
